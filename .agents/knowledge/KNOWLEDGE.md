# Knowledge Index

Non-spec knowledge extracted from the codebase. Two indexes:

- **Failures** — match a reported symptom or error fragment, then load only the linked file. Check here first on any unexpected failure.
- **Constraints & semantics by area** — durable rules, invariants, and non-obvious behavior. Scan the relevant area before changing code there.

Decisions with durable rationale live in [`specs/adr/`](../../specs/adr/README.md); schema semantics live in `specs/state-model.md` and `docs/concepts/`. Source files carry no prose comments (ADR 0006).

## Failures

| Category | Match symptoms / keywords | File |
| --- | --- | --- |
| Ansible / sudo | `Duplicate become password prompt`; apply hangs mid-play on sudo | [sudo-ansible-duplicate-prompt.md](sudo-ansible-duplicate-prompt.md) |
| Ansible / sudo | `sudo: sorry, you must have a tty to run sudo`; `sudo: a password is required`; `Install controller OS packages` | [ansible-sudo-requiretty.md](ansible-sudo-requiretty.md) |
| Ansible / remote tmp | `can't open file '<home-dir>/.ansible/tmp`; `[Errno 13] Permission denied`; `No space left on device`; `AnsiballZ_setup.py`; `AnsiballZ_uri.py` | [ansible-remote-tmp-permission.md](ansible-remote-tmp-permission.md) |
| Ansible / sudo | `bootwright apply <target>` auto-sudo cannot find ansible; `ModuleNotFoundError` under sudo | [pip-user-sudo-pythonpath.md](pip-user-sudo-pythonpath.md) |
| Ansible / sudo | `Ensure tmp working directory exists`; permission denied under root-owned controller temp paths | [ansible-controller-clis-root-temp.md](ansible-controller-clis-root-temp.md) |
| Ansible / Galaxy | `Unexpected Exception`; `'results'`; `Skipping Galaxy server`; `community.general` | [ansible-galaxy-results-build.md](ansible-galaxy-results-build.md) |
| Ansible / embed | Extracted bundle missing `_respawn.py`, `__init__.py`, dot/underscore files | [ansible-embed-underscore-files.md](ansible-embed-underscore-files.md) |
| Ansible / roles | `bootwright_current_cluster is undefined`; dynamic role import fails | [ansible-dynamic-role-dispatch.md](ansible-dynamic-role-dispatch.md) |
| Ansible / runtime | `Module result deserialization failed`; `rc=-15`; cleanup killed Ansible wrapper | [ansible-module-wrapper-pkill.md](ansible-module-wrapper-pkill.md) |
| Ansible / runtime | `Could not install packages due to an OSError`; `[Errno 2] No such file or directory`; `install ansible-core` | [ansible-managed-venv-rebuild.md](ansible-managed-venv-rebuild.md) |
| Tests / temp paths | `stdout missing ANSIBLE_SSH_CONTROL_PATH_DIR`; `expected a run-scoped control path`; `a parent we own should be repaired rather than abandoned`; `managed root path ... must not contain symlink`; disk-backed `TMPDIR` has a long path | [make-check-and-ci-gates.md](make-check-and-ci-gates.md) |
| Go / temporary worktree | `error obtaining VCS status: exit status 128`; `Use -buildvcs=false`; `failed caching data: open .../.cache/staticcheck/...: read-only file system`; linked worktree below `/tmp`; an empty parent `/tmp/.git` directory; read-only default staticcheck cache | [make-check-and-ci-gates.md](make-check-and-ci-gates.md) |
| Go / race test | `DATA RACE`; `phasedCSVRunner.Run`; `TestApplyAndWaitShareOneReadinessDeadline`; concurrent add-on readiness checks | [addon-oc-runner-output.md](addon-oc-runner-output.md) |
| Go / clean checkout | `go-test-clean-checkout`; `TestApplyDestroySafetyMatrix`; `embedded ansible bundle is empty`; `missing ansible_bundle.zip` | [make-check-and-ci-gates.md](make-check-and-ci-gates.md) |
| Go / CI fixture | `TestApplyDestroySafetyMatrix`; `a refusal must name a backticked exact Bootwright command`; `Installer tools`; `oc: not found` | [make-check-and-ci-gates.md](make-check-and-ci-gates.md) |
| Ansible / callback | `Callback dispatch 'v2_runner_on_skipped' failed for plugin 'default'`; `Build proxy environment facts` | [ansible-callback-skipped-pin.md](ansible-callback-skipped-pin.md) |
| Ansible / lint | `Skipping filter plugins in bootwright.core.test_*`; `has no attribute 'FilterModule'` | [ansible-filter-test-placement.md](ansible-filter-test-placement.md) |
| Ansible / conditionals | `Conditional result (True) was derived from value of type 'dict'`; `Conditionals must have a boolean result`; `<CLI option '-e'>` | [ansible-strict-boolean-conditionals.md](ansible-strict-boolean-conditionals.md) |
| Ansible / transfer | `Fetch generated agent ISO to local cluster runtime state`; large file fetch appears stuck; `.openshift_install.log` idle after ISO generation | [ansible-fetch-become-large-file.md](ansible-fetch-become-large-file.md) |
| Ansible / packages | `Failed to download metadata for repo`; `Cannot download repomd.xml`; `There are no enabled repositories`; `All mirrors were tried`; IBM Ceph declared-repository read or package-content preflight fails | [ansible-dnf-unavailable-repo.md](ansible-dnf-unavailable-repo.md) |
| Provider / BMC | Apply hangs at BMC wait tasks; port already in use after provider rename; same-name provider in another context; foreign sushy unit or libvirt pool would be redefined/deleted | [stale-bmc-port-wait-hang.md](stale-bmc-port-wait-hang.md), [substrate-ownership-markers.md](substrate-ownership-markers.md) |
| OpenShift / OKD clusters | Disconnected install fails TLS; agent never reaches SSH; image pull x509 error | [disconnected-trust-bundle-policy.md](disconnected-trust-bundle-policy.md) |
| OpenShift / OKD clusters | Mirror push x509 SAN mismatch after self-signed cert spec change | [self-signed-cert-drift.md](self-signed-cert-drift.md) |
| OpenShift / OKD clusters | Agent ISO cache stale; `cannot generate ISO image due to configuration errors` | [openshift-agent-iso-cache.md](openshift-agent-iso-cache.md) |
| OpenShift / OKD clusters | `Missing install-config.yaml`; `Fail when install-config is missing`; boot task after successful ISO generation | [openshift-install-config-consumed.md](openshift-install-config-consumed.md) |
| OpenShift / OKD clusters | `Stage installer extra manifests on controller`; `Could not find or access .../runtime/installer/openshift/` | [container-cluster-ansible-flow.md](container-cluster-ansible-flow.md) |
| OpenShift / OKD clusters | `SQUASHFS error: Unable to read page`; `Unable to read fragment cache entry`; agent API never initializes after virtual media eject | [openshift-agent-iso-squashfs-detach.md](openshift-agent-iso-squashfs-detach.md) |
| OpenShift / OKD clusters | `v2GetClusterNotFound`; `Writing image to disk: 100%`; node boots agent ISO again | [openshift-agent-iso-reboot-loop.md](openshift-agent-iso-reboot-loop.md) |
| OpenShift / OKD clusters | `Only platform none and external supports 1 ControlPlane and 0 Compute nodes`; SNO renders `platform.baremetal` | [openshift-sno-platform-none.md](openshift-sno-platform-none.md) |
| OpenShift / OKD clusters | `hosts[0].interfaces: Required value`; libvirt profile-backed nodes have no MAC inventory | [openshift-agent-host-interfaces.md](openshift-agent-host-interfaces.md) |
| OpenShift / OKD clusters | `Could not resolve hostname`; `Bootstrap Kube API never initialized`; `no such host`; controller DNS missing; `providedBy` or `externalVip` | [external-dns-bootstrap.md](external-dns-bootstrap.md) |
| OpenShift / OKD clusters | `failed to progress after all hosts available`; `Cluster is ready for install` then an immediate wait failure; one declared node missing from every validation line | [openshift-agent-cluster-stuck-in-ready.md](openshift-agent-cluster-stuck-in-ready.md) |
| OpenShift / OKD clusters | `bootstrap process timed out: context deadline exceeded`; degraded `ingress`/`monitoring`/`openshift-apiserver` dump; rendezvous host parked at `Waiting for bootkube`; one of two concurrently installing clusters finishes and the other does not | [openshift-agent-wait-installer-window.md](openshift-agent-wait-installer-window.md) |
| OpenShift / OKD clusters | `updated status from installing-in-progress to error (… stage Joined took longer than expected 1h0m0s)`; `Cluster has hosts in error`; the rendezvous node never appears in `oc get nodes`; bootstrap static pods still in its `/etc/kubernetes/manifests` | [openshift-agent-host-error-strands-rendezvous.md](openshift-agent-host-error-strands-rendezvous.md) |
| OpenShift / node config | a node demoted from `role: infra` keeps `node-role.kubernetes.io/infra` and its `NoSchedule` taint forever; a deleted `nodes[].labels` entry stays live; `spec.taints[1]: Duplicate value`; `taints must be unique by key and effect pair`; `Apply failed with 1 conflict` on `nodeconfig.<cluster>.apply`; the `infra` MachineConfigPool outlives its last infra node | [node-config-day2-reconcile.md](node-config-day2-reconcile.md) |
| Libvirt / network | API VIP unreachable; `Bootstrap Kube API never initialized` | [libvirt-vip-bootstrap.md](libvirt-vip-bootstrap.md) |
| Libvirt / network | UUID mismatch; bridge already in use; stale libvirt XML | [libvirt-network-drift.md](libvirt-network-drift.md) |
| Redfish / boot | `InsertMedia` fails; `did not report the requested agent ISO`; `Inserted=False`; `VerifyCertificate PATCH status=412`; `ssl.SSLError`; emulator HTTPS mismatch; virtual media path mismatch; `Verify running libvirt virtual media source is absent`; `Confirm staged agent ISO fetch URL is reachable`; `HEAD status 404`; multi-GB ISO-shaped `/tmp/tmp*` files | [redfish-virtual-media.md](redfish-virtual-media.md) |
| Redfish / boot | VirtualMedia discovery reports `status=403`; direct `curl -u` to the same BMC URL succeeds; play-level proxy environment intercepts Redfish | [redfish-proxy-bypass.md](redfish-proxy-bypass.md) |
| Redfish / boot | `Stage agent ISO at the BMC's fetch location`; `Gathering Facts`; `UNREACHABLE`; `Failed to connect to the host via ssh`; `localhost -> bastion`; wrong `ansible_user` | [redfish-local-artifact-staging.md](redfish-local-artifact-staging.md) |
| Redfish / boot | Reset(On) returns 204 but VM stays `shut off`; install loops on `no route to host` | [redfish-power-on-silent-noop.md](redfish-power-on-silent-noop.md) |
| Redfish / boot | `Wait for node SSH to confirm live ISO boot complete`; `Timeout when waiting for [<IP>:22]`; legacy `elapsed=600`; `Confirm node SSH became reachable after live ISO boot`; one physical node times out while peers continue | [redfish-boot-flow-quirks.md](redfish-boot-flow-quirks.md) |
| Redfish / TPM | `refusing encrypted installer boot`; `TrustedModules is not reported`; `no TPM2_0 module reports State=Enabled and Health=OK`; encrypted bare-metal preflight refuses before boot | [disk-encryption-tpm.md](disk-encryption-tpm.md) |
| Managed OS / libvirt | `Scan managed OS SSH host key`; `Connection reset by peer`; VM console shows wrong RHEL version; `I/O error, dev vda`; `XFS (vda3): log I/O error` | [managed-os-libvirt-stale-disk.md](managed-os-libvirt-stale-disk.md) |
| Python / Ansible | Python 3.12 CIDR check returns false; VIP not matched to bridge CIDR | [python-312-cidr-filter.md](python-312-cidr-filter.md) |
| Add-ons / OLM | readiness check JSON unmarshal fails; oc deprecation/TLS/auth warnings in captured output; `csvSucceeded` decode error; panic in io.MultiWriter during readiness poll; quiet runner nil writer | [addon-oc-runner-output.md](addon-oc-runner-output.md) |
| Add-ons / OLM | IBM Fusion CatalogSource stays `Unknown` or `TRANSIENT_FAILURE`; nothing appears in Software Catalog; catalog registry pod rejected by pod security | [fusion-catalog-security-context.md](fusion-catalog-security-context.md) |
| Add-ons / Data Foundation | `no matches for kind "StorageCluster" in version "ocs.openshift.io/v1"`; `attach-external-storage` applies its Secret then fails on the StorageCluster; `oc get storageclusters` finds nothing after the step "ran"; one OpenShift cluster fails while its twin succeeds | [data-foundation-external-attach.md](data-foundation-external-attach.md) |
| Add-ons / Data Foundation | no `ocs-external-storagecluster-ceph-rgw` StorageClass; no `rgw-admin-ops-user` Secret; NooBaa default backingstore is type `pv-pool` instead of `s3-compatible`; a declared `objectGatewayRef` yields no S3 while every object reports healthy | [data-foundation-external-attach.md](data-foundation-external-attach.md) |
| Add-ons / Data Foundation | NooBaa holds `phase: Configuring` with reason `TemporaryError`; `got error when trying to create bucket nb.<id>`; `tls: failed to verify certificate: x509: certificate signed by unknown authority` against the RGW; StorageCluster `Progressing` / `NoobaaInitializing` while the CephCluster is `Connected` and `HEALTH_OK` | [data-foundation-external-attach.md](data-foundation-external-attach.md) |
| Add-ons / Data Foundation | a StorageCluster condition frozen at an old `lastHeartbeatTime` while its siblings keep ticking; readiness blames `CephCluster resource is not reporting status` when the CephCluster reports `Connected` | [addon-oc-runner-output.md](addon-oc-runner-output.md) |
| Add-ons / Data Foundation | `error setting modifier for [client.healthchecker] type=key`; `Malformed input [buffer:3]`; `failed to get external ceph mon version`; `monclient: keyring not found`; `[errno 5] RADOS I/O error`; `ExternalClusterStateError`; StorageCluster stuck `Progressing` for the whole readiness budget; exported cephx key starts `Ag` instead of `AQ` | [data-foundation-external-attach.md](data-foundation-external-attach.md) |
| Ansible / runtime | `'ansible_distribution_major_version' is undefined` while resolving `bootwright_ceph_provider`; set_fact mapping with nested {{ hostvars }}/ansible_* resolves empty or wrong; fact-independent leaf succeeds | [ansible-core-eager-setfact.md](ansible-core-eager-setfact.md) |
| Ansible / runtime | play frozen mid-task with no error or timeout; host mid-reboot or powered off; SSH banner printed then stall; a TASK banner showing fewer `ok:` lines than the play has hosts; `podman run --entrypoint stat` running for 30+ minutes on one node; `Ceph state-changing command ... exceeded its ... safety timeout`; `rc=124`; `rc=137`; exact resolved invocation; `ConnectTimeout=15`; `ServerAliveInterval=15`; `ServerAliveCountMax=3`; `ssh_common_args` | [ansible-ssh-liveness-timeouts.md](ansible-ssh-liveness-timeouts.md) |
| Ansible / SSH options | an `ansible.cfg` `ssh_common_args` option has no effect on machine hosts; keepalives missing despite the cfg line; host-key verification behaves as `accept-new` although the inventory pins `StrictHostKeyChecking=yes`; `ansible_ssh_common_args`; `ansible_ssh_extra_args` | [ansible-ssh-common-args-precedence.md](ansible-ssh-common-args-precedence.md) |
| Bare-metal & managed-OS install | Key exchange type c25519 is not allowed in FIPS mode; ssh-keyscan retries forever; StrictHostKeyChecking=accept-new; ssh-keygen -R; ecdh-sha2-nistp256 | [fips-ssh-keyscan-hostkey.md](fips-ssh-keyscan-hostkey.md) |
| Bare-metal & managed-OS install | Could not find or access; mkksiso; install_media.yml; remote_src: false; sourceOnTarget; source ISO missing on controller; bootwright media add | [managed-os-install-media-missing.md](managed-os-install-media-missing.md) |
| Bare-metal & managed-OS install | StrictHostKeyChecking fails forever after a reinstall; `No ECDSA host key is known`; stale known_hosts pin; host key changed on freshly installed node; parallel nodes lose shared known_hosts entries or FQDN aliases; `flock`; `ssh-keygen -R` | [managed-os-ssh-trust.md](managed-os-ssh-trust.md) |
| Bare-metal & managed-OS install | Anaconda never runs; node boots existing OS from disk after Redfish power-on; foreign OS answers SSH at auth/ownership check; `BootSourceOverrideEnabled=Once` overwritten to Hdd during POST; `readiness.type=none`; `setBootSource` | [redfish-post-boot-disk-override.md](redfish-post-boot-disk-override.md) |
| Ceph storage | proxy = _none_; repomd.xml unreachable; baseos/appstream; unable to get local issuer certificate; sslcacert redhat-uep.pem; repo_ca_cert; subscription-manager refresh; rhsm.conf 0600; bootwright-proxy-ca.pem; bootwright-satellite-ca.pem; bootwright-cephadm-registry.pem | [rhsm-proxy-and-repo-ca.md](rhsm-proxy-and-repo-ca.md) |
| Ceph storage | CEPHADM_STRAY_DAEMON after a successful multi-host install; stray mon on a non-mon-role host; `ceph orch ps` looks correct but `ceph health detail` warns | [ceph-mon-mgr-default-placement-race.md](ceph-mon-mgr-default-placement-race.md) |
| Ceph storage / IBM | `Inspect IBM Call Home manager module state`; `ceph mgr module ls --format json` prints complete JSON then reaches 120 seconds and exits `rc=124`; `Disable IBM Call Home manager module`; `ceph orch deny call-home-enabled`; `Call home agent module disabled and health warning for call home enablement cleared`; command then reaches 300 seconds and exits `rc=124` | [ceph-distribution-packaging.md](ceph-distribution-packaging.md) |
| Ceph storage / runtime | `crun: systemd failed to install eBPF device filter on cgroup`; `Non-zero exit code 126 from /bin/podman run`; `Failed to extract uid/gid for path /var/lib/ceph`; `CEPHADM_DAEMON_PLACE_FAIL`; `CEPHADM_REFRESH_FAILED`; `cephadm ceph-volume` failed on one host; `bpftool cgroup tree` answers `Invalid argument`; one node (often a VM or the stretch arbiter) deploys no daemon of any type while its peers are fine | [ceph-node-container-runtime-lockdown.md](ceph-node-container-runtime-lockdown.md) |
| Ceph storage | `Wait for every declared Ceph mon to join the monmap` exhausts its retries; `Assert every declared Ceph mon joined the monmap`; `Filtered out host`; `does not belong to mon public_network(s)`; `ceph orch ls --service_type mon` shows every declared host but `size:` above `running:`; a stretch tiebreaker/arbiter on a subnet of its own never deploys a mon; `ENOENT: mon.<name> does not exist` from `ceph mon set_location` or `ceph mon enable_stretch_mode`; a corrected `publicCIDRs` still fails the same way on re-apply | [ceph-mon-public-network-placement.md](ceph-mon-public-network-placement.md) |
| Ceph storage | `Write RGW ingress TLS certificate specs`; `lookup plugin 'ansible.builtin.file' failed`; RGW ingress `certificatePath` or `keyPath` under task `artifacts/runtime/secrets` is missing | [ceph-monitoring-service-specs.md](ceph-monitoring-service-specs.md) |
| Ceph storage | `Error EINVAL: Service Spec is not an (JSON or YAML) object. got "[{'placement': …}]"`; `rc=22` from `ceph orch apply -i`; `Apply RGW ingress TLS certificate spec`; `Apply management service spec`; the printed spec shows `-----END CERTIFICATE-----\n-----BEGIN` with a literal backslash-n at the cert/key seam | [ceph-cephadm-bootstrap-contract.md](ceph-cephadm-bootstrap-contract.md) |
| OpenShift / OKD clusters | Installer log reports a version the cluster does not declare; `Using internal constant for release image`; `Failed when progressing towards operator: <unexpected version>`; a release bump installs the previous release with no error | [openshift-installer-release-skew.md](openshift-installer-release-skew.md) |
| Ceph storage | `Error EINVAL: ServiceSpec: __init__() got an unexpected keyword argument 'ssl_certificate'`; `Refuse a management service spec cephadm reported an error for`; a `mgmt-gateway`/`oauth2-proxy` document is refused while the rest of the apply is fine; `Refuse a management gateway on a Ceph release that has no such service`; `requires Ceph 20 or later` from `bootwright validate` | [ceph-cephadm-bootstrap-contract.md](ceph-cephadm-bootstrap-contract.md) |
| Secrets / preflight | `generated self-signed certificate <name>: certificate is not PEM-encoded` immediately after `bootwright secret generate --renew` | [secret-index-resolution.md](secret-index-resolution.md) |
| Preflight / reachability | `bootwright preflight all` exits `4` on a never-applied estate; `PLAY RECAP` shows `unreachable=1` for every `storage__<cluster>__<machine>`; the named failing task is `Resolve storage nodes on this host` but that task succeeded; `Failed to connect to the host via ssh`; `Name resolution` skips a provided arbiter that is a topology node | [preflight-hosts-not-installed-yet.md](preflight-hosts-not-installed-yet.md) |
| OpenShift / OKD clusters | quay.io/openshift-release-dev/ocp-v4.0-art-dev; disconnected nodes pull quay.io despite mirror; imageDigestSources; release component images; NeverContactSource; openshift/release-images | [disconnected-release-image-artdev.md](disconnected-release-image-artdev.md) |
| OpenShift / OKD clusters | Could not open <iso>: Permission denied; libvirt-direct attach fails to start guest; publish_target.yml mode 0711; qemu cannot traverse per-token directory; stagePath publish token; no_log redaction | [openshift-agent-iso-publish-permissions.md](openshift-agent-iso-publish-permissions.md) |
| OpenShift / OKD clusters | unchanged apply stops KubeVirt VMs; guest console unavailable after apply; CatalogSource unavailable after guest VM reconcile; spec.running false; runStrategy Manual; Refuse to modify foreign KubeVirt DataVolume; pre-exact-label agent ISO; no matching resources found; kubectl wait --for=condition=Ready vmi; VM boots stale agent ISO on retry; virtctl start already running no-op; virtctl image-upload stamps no labels; bootwright.io/managed-by; bootwright_kubevirt_agent_iso_size 4Gi upload fails; cdi-uploadproxy; tls failed to verify certificate; x509 certificate signed by unknown authority | [openshift-kubevirt-agent-boot.md](openshift-kubevirt-agent-boot.md) |
| OpenShift / KubeVirt storage | `installation stage Writing image to disk did not sufficiently progress in the last 30m0s`; virtual machine disk pathologically slow on Ceph RBD; clone stuck at `CloneInProgress` and never `Failed`; PVC lands on `volumeMode: Filesystem` against a Block class; `Cannot update DataVolume Spec`; spec.pvc vs spec.storage; StorageProfile inference; one master fails late while the rest install; `CheckSafeToScaleCluster found 2 healthy member(s) out of the 3 required`; a disk created before the fix is reused and refused with `--mode rebuild` | [cdi-datavolume-spec-pvc-vs-storage.md](cdi-datavolume-spec-pvc-vs-storage.md) |
| OpenShift / KubeVirt preflight | `memcache.go:265`; `the server could not find the requested resource`; repeated KubeVirt API and network failures | [kubevirt-preflight-api-probes.md](kubevirt-preflight-api-probes.md) |
| OpenShift / KubeVirt render | live run renders the durable `{{ bootwright_clusters_dir }}/<host>/secrets/kubeconfig` instead of the materialized copy; dry run renders no `kubevirt.kubeconfig`; `KubeVirtHostKubeconfigPaths` nil vs empty | [kubevirt-host-kubecfg-paths-nil-vs-empty.md](kubevirt-host-kubecfg-paths-nil-vs-empty.md) |
| OpenShift / KubeVirt tooling | `Installed virtctl does not match host cluster KubeVirt version`; identical `GitVersion` and server version; `Client Version: version.Info` | [virtctl-version-output-parsing.md](virtctl-version-output-parsing.md) |
| OpenShift / virtual-machine destroy | VM teardown takes one full deletion duration per node; serialized destroy; recorded guest remains after destroy; host kubeconfig missing before VM deletion; `KubeVirt host cluster ... is unreachable`; `--authorize unreachable-nodes cannot prove them absent`; same-name foreign VM/DataVolume/PVC; forbidden or empty PVC probe | [destroy-scoping-and-sweeps.md](destroy-scoping-and-sweeps.md#teardown-ordering-and-per-cluster-fan-out), [substrate-ownership-markers.md](substrate-ownership-markers.md) |
| CLI / root gate | `current context "<name>" is not available in shared storage`; `lstat /var/lib/bootwright/contexts/<name>: permission denied`; no `SUDO password:` prompt; the same `cluster exec`/`machine exec` works until a `-h`, `--help`, or `help` is passed to the remote command; every `container-cluster oc`/`kubectl`/`kubeconfig` or `storage-cluster replace-arbiter` invocation fails that way | [cli-root-gate-sudo-reexec.md](cli-root-gate-sudo-reexec.md) |
| CLI / root gate | `SUDO password:` immediately followed by `SSH sudo password:`; one `--ssh-ask-sudo-password` run asks for the same password twice | [cli-root-gate-sudo-reexec.md](cli-root-gate-sudo-reexec.md) |
| CLI tests / password prompt | `TestAskSudoPasswordRefusesAnEmptyAnswer` or `TestAskSudoPasswordPromptsOnStderrBeforeTheCommandRuns` hangs only when `go test` has a controlling TTY | [cli-root-gate-sudo-reexec.md](cli-root-gate-sudo-reexec.md) |
| CLI / SSH | `cluster rsh`; `declares no SSH access (spec.access.ssh)`; OpenShift node Machine has no access block; encrypted node SSH key; `machine rsh`/`machine exec` on a Ceph topology node fails `Permission denied (publickey)` while the node is otherwise healthy; `machine list` prints one login and `machine rsh` tries another | [cluster-node-ssh-access.md](cluster-node-ssh-access.md) |
| CLI / media | `bootwright media list` takes minutes; listing time grows with total stored ISO size; every ISO is read despite requesting inventory only | [managed-os-media-staging.md](managed-os-media-staging.md) |
| Rendering & templates | hostname verification failed after SAN edit; artifacts-openssl.cnf.j2; bootwright_artifacts_tls_openssl_cnf is changed; ssl_ciphers verbatim injection; artifacts-nginx.conf.j2; DEFAULT:@SECLEVEL=0; tlsVersionsAscending; legacy iBMC InsertMedia TLS failure | [artifact-server-tls-render.md](artifact-server-tls-render.md) |
| Infra components | REGISTRY_AUTH_HTPASSWD_PATH panic; crash-loop under restart_policy; net.ipv4.ip_nonlocal_bind; squid refuses to run as root UID 1000; Address not found; Cannot assign requested address; Cannot find device; dns enable='no'; bind-dynamic grabs :53; dhcp-option=option:ntp-server IPv4 only | [infra-component-service-gotchas.md](infra-component-service-gotchas.md) |
| Infra components / destroy | `Require a conclusive firewalld probe before artifact-server teardown`; assertion `selected_listener_ports | length == 0 or bootwright_firewalld_available` fails when firewalld is absent or stopped | [substrate-ownership-markers.md](substrate-ownership-markers.md#conclusive-firewalld-absence-is-not-a-teardown-blocker) |
| Infra components / transition | `Require closed infra-component transition and survivor ledgers`; assertion `bootwright_infra_transition_desired_ports.valid` is false for an artifact server; artifact-server-only destroy selects no desired service | [render-internal-vocabularies.md](render-internal-vocabularies.md) |
| Apply / scope | `infra-component.<host> requires unavailable capability machine.os-ready:<host>`; scoped `--clusters` plan retains an unconsumed InfraComponent after dropping its Machine | [apply-task-graph-planning.md](apply-task-graph-planning.md) |
| Apply / scheduler | under an explicit cluster-install cap, a bare-metal cluster sits at `waiting for a cluster install slot` while a parked KubeVirt child builds its ISO; or Ceph managed OS and one OCP boot run while a second independent OCP root is bare `[PENDING]` / `waiting for Redfish slots`; `4 RUNNING` despite only two executing task cohorts | [apply-cluster-install-slot.md](apply-cluster-install-slot.md) |
| Rendering & templates | too many colons; Port could not be cast to integer value; bracketRedfishHost; net.JoinHostPort; TestManagedProxyURLBracketsIPv6; urlencode leaves / unescaped; replace('/', '%2F'); userinfo percent-encoding | [url-authority-gotchas.md](url-authority-gotchas.md) |
| Ceph storage / destroy | `Task failed: Incorrect sudo password` right after a successful "Storage clusters" destroy step; a later destroy retry then fails `Failed to connect to the host via ssh: cephadm@<host>: Permission denied (publickey,...)`; a FIRST destroy fails that way on only some Ceph nodes while apply reached the cephadm phase on all of them; `Failed to lookup user cephadm`; `Failed to lookup user bootwright`; `getpwnam(): name not found`; `Deauthorize the machine access key`; `Reauthorize the machine access key for the storage node install account`; a destroy aborts on ONE node right after "Restore root SSH login" succeeded on every node; apply --mode rebuild afterwards performs no OS reinstall; a successful full destroy with `--authorize unreachable-nodes` is followed by `Refuse to reinstall a reachable managed OS Bootwright cannot authenticate to` on every root-revoked Ceph node | [destroy-scoping-and-sweeps.md](destroy-scoping-and-sweeps.md#storage-node-access-revocation) |
| Ceph storage / devices | `Refusing to reclaim`; `it could not be probed or is mounted/in use ()` with EMPTY parentheses; `lsblk: /dev/nvmeXn1: not a block device`; `rc=32`; a declared OSD device that exists on no node | [ceph-osd-device-safety.md](ceph-osd-device-safety.md) |
| Ceph storage / devices | `Refusing to reclaim`; `it carries LVM or dm-crypt holders`; `Refusing to wipe storage device`; `is not recorded as a Bootwright OSD device on this node`; `no device will be reclaimed` from a post-destroy `--reclaim-devices` apply; `cephadm shell` answers `/var/lib/ceph is missing` or `RADOS object not found`; drain-the-OSD advice with no cluster to drain from | [ceph-osd-device-safety.md](ceph-osd-device-safety.md) |
| Ceph storage / install | `did not create the declared OSDs: expected exactly N OSD(s) 'in', observed N (up N)` with observed EQUAL to expected; the attempt count far short of its budget (`after 28 of 180 attempts`); every poll sample eating its full 120 s | [ceph-osd-device-safety.md](ceph-osd-device-safety.md#constraint-the-readiness-verdict-reads-the-sampled-stdout-never-the-samplers-exit-status) |
| Build / container | `embedded ansible bundle is empty (rebuild bootwright via 'make build'): missing ansible.cfg` from a binary built with the Containerfile; intermittent — only on a COLD build with no BuildKit layer cache | [container-build-and-stamping.md](container-build-and-stamping.md) |
| Build / container | `bootwright version` reports `-dirty` after a container build from a pristine checkout at a tagged commit; proxy credentials appear as `--build-arg` although `proxy.env.example` promises they never do | [container-build-and-stamping.md](container-build-and-stamping.md) |
| Ceph storage / destroy | `Preserving /etc/ceph, /var/lib/ceph and /var/log/ceph on <peer>: foreign Ceph cluster fsid(s) <fsid> not owned by <cluster>` where that fsid IS the cluster being destroyed; a green destroy that leaves every peer's cluster intact and the seed unable to prove ownership next time; `Refuse a non-seed storage host still carrying a cluster the seed resolved no fsid for` | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md) |
| Ceph storage / destroy | a GREEN `destroy --authorize all` whose next apply refuses one node's OSD disks (`carries data signatures`, `holds foreign data`, zero OSDs at readiness) while every peer is clean; usually the seed; a second destroy cleans it; nothing in the destroy output names the node | [ceph-osd-device-safety.md](ceph-osd-device-safety.md#constraint-the-per-host-wipe-verification-is-not-the-release-proof--the-settle-gate-is) |
| Ceph storage / destroy | after a GREEN destroy, `pvs -o pv_name,vg_name,lv_name` on one node still lists `ceph-<uuid>` volume groups with `osd-block-<uuid>` LVs while every peer prints nothing; the next destroy then refuses at `Refusing to tear down the cephadm cluster on <seed>` and only `--recover-ceph-ownership <cluster>=<fsid>` gets past it; the final scan prints the late VGs but filters survivors through the initial device list | [ceph-osd-device-safety.md](ceph-osd-device-safety.md#constraint-the-release-proof-reads-the-node-not-the-device-list-the-run-resolved--destroy_stepslvm_sweepyml) |
| Ceph storage / destroy | `Refusing to release a storage node a Ceph daemon outlived`; `Refusing to release a storage node whose wiped device was signed again`; `Refusing to release a storage node whose surviving-daemon probe did not run` | [ceph-osd-device-safety.md](ceph-osd-device-safety.md#constraint-the-per-host-wipe-verification-is-not-the-release-proof--the-settle-gate-is) |
| Ceph storage / destroy | `[OK] destroy: complete` with a `[WARN] partial destroy` while one node still runs its OSDs; a skipped node that answered SSH the whole time; `Logical volume ceph-<uuid>/osd-block-<uuid> in use`; `wipefs: probing initialization failed: Device or resource busy`; `PV /dev/nvmeXn1 is used by VG ceph-<uuid> so please use vgreduce first`; `Refuse a storage host that answers but rejects every teardown identity`; `Refusing to wipe a Ceph device whose volume group is still open` | [ceph-node-access-privileged-channel.md](ceph-node-access-privileged-channel.md), [ceph-osd-device-safety.md](ceph-osd-device-safety.md) |
| Ceph storage / destroy | `--recover-ceph-ownership cannot authorize`; `no Bootwright owner record for declared seed host`; successful apply left no controller StorageCluster ownership record | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md) |
| Ceph storage / ownership | a malformed, foreign-context, wrong-seed, wrong-manager, empty-fsid, or reference record at the expected controller path is accepted by apply rebuild, destroy, survivor adoption, or `--recover-ceph-ownership`; `rm-cluster --zap-osds` can follow an existence-only check | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md) |
| Apply / ownership | a valid JSON ownership record copied from another context, stamped with a wrong owner/API, or placed at a noncanonical path is invisible to apply and is overwritten by the generic ownership writer | [apply-destroy-authorization-guards.md](apply-destroy-authorization-guards.md#adding-to-the-destructive-surface--what-to-do-and-what-fails-if-you-dont) |
| Ceph storage / destroy | a SECOND `bootwright destroy` sits for many minutes after `Refuse to wipe present Ceph devices without a valid Bootwright OSD record` with no further banner; the stall is really the next task, `Check Ceph fsid on seed host`, pulling a container image or retrying dead mons; `Refusing to tear down the cephadm cluster on <seed>` naming no specific missing evidence | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md), [ansible-ssh-liveness-timeouts.md](ansible-ssh-liveness-timeouts.md) |
| Ceph storage / node reuse | `Refuse a storage node that still runs a cephadm cluster this apply does not own`; `TCP Port(s) '0.0.0.0:9100' required for node-exporter already in use`; `no such container "ceph-<fsid>-<daemon>"` immediately before that bind failure; one service one daemon short (`node-exporter (6/7)`) on a node a previous teardown skipped or failed on; `systemctl` shows a `ceph-<other fsid>@<daemon>.service` still running next to this cluster's units | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md) |
| Ceph storage | `Assert declared Ceph services reached their daemon count`; `mgmt-gateway (N/M)` stuck partway with every other service full; `ceph orch ps` shows mgmt-gateway daemons only on non-mgr hosts; `CEPHADM_DAEMON_PLACE_FAIL`; `Cannot bind to IP 0.0.0.0 port 8443: [Errno 98] Address already in use`; `TCP Port(s) '0.0.0.0:8443' required for mgmt-gateway already in use`; `MGR_MODULE_ERROR`; `Module 'dashboard' has failed: Port 8080 not free`; cephadm log dominated by `Reconfiguring mgmt-gateway.<node> deps` with `diff {'ssl_cert: <md5>', 'ssl_key: <md5>', 'certificate_source: inline'}` or `diff {'certificate_source: cephadm-signed'}`; gateways stuck `starting` with `pending_daemon_config: true`; `mgmt-gateway-spec-dependency-recording requires ... exposure: http` from `bootwright validate`; `ceph orch ls --export` shows `ssl: true` on a gateway applied with `ssl: false`; gateway loop returns after a mgr failover; dashboard serves the `cephadm-root-<fsid>` certificate or 503 after the loop stops; `Could not reach Prometheus's API on https://<vip>:29443/internal/prometheus` with `No route to host` | [ceph-monitoring-service-specs.md](ceph-monitoring-service-specs.md) |
| Ceph storage | apply green but declared services never deployed; `ceph orch ls` shows `rgw.odf 0/6`, `mds.odf-cephfs 0/6`, `prometheus 1/4` with `REFRESHED -` and `VERSION <unknown>`; HEALTH_OK despite zero daemons; `bootwright_ceph_service_readiness_retries` exhausted | [ceph-service-rollout-gate.md](ceph-service-rollout-gate.md) |
| Ceph storage / node access | `sudo: sorry, you must have a tty to run sudo` on a Ceph topology node; `Resolve storage node privileged execution`; `Probe privileged execution on the storage node without a terminal`; `cannot run a privileged command`; `Create the storage node orchestration account`; `Require the storage node orchestration account before any root revocation` fails though `/etc/sudoers.d/60-bootwright-<user>` is present and 0440 root:root; `PTY allocation request failed on channel 0`; `rc=255`; `PermitTTY no`; `ignore_local_sudoers`; `sudo -ll -U`; the sudoers grant is re-staged and re-visudo'd with `changed=` on every apply | [ceph-node-access-privileged-channel.md](ceph-node-access-privileged-channel.md) |
| Go / tests | a Go test package takes minutes and its runtime tracks machine load; `ps` shows real `ansible-playbook` and `ssh` processes against fixture hosts such as `bastion.bootwright.test`; a loaded run fails where an idle one passes | [go-test-spawns-real-ansible.md](go-test-spawns-real-ansible.md) |
| Provider / vSphere privileges | `Unable to find datacenter "…"`, `Unable to find cluster "…"`, `Unable to find resource_pool "…"`, `Could not find a template named …` although the names are correct; a VM Bootwright just created is refused on the next apply with `it already exists but carries no Bootwright ownership marker`; the annotation `ReconfigVM_Task` fails with `op: 'annotation'`; destroy demands `--authorize unowned-vms` for a VM Bootwright created; staged ISOs pile up on the datastore although destroy reports green; a green `bootwright preflight` followed by a mid-apply vCenter permission failure | [vcenter-minimum-privileges.md](vcenter-minimum-privileges.md) |
| Bare-metal & managed-OS install | a cloned vSphere machine never answers SSH; `Wait for cloned managed OS SSH port` times out; the clone boots with the template's hostname; the VM has no `guestinfo.metadata` / `guestinfo.userdata` in `extraConfig`; cloud-init did not run on first boot; `/etc/cloud/cloud-init.disabled` already in the template; `preserve_hostname: true`; hostname or IP set non-deterministically on first boot (GOSC racing cloud-init) | [vsphere-guest-personalization.md](vsphere-guest-personalization.md) |
## Constraints & semantics by area

### Apply, destroy & drift engine

| Topic | File |
| --- | --- |
| The registries and guard tests that keep the apply/destroy authorization contract closed over new code: the ApplyTaskKinds registry, the reconfigure-only allowlist, the apply-kind→destroy-kind reset map, the record round-trip pin, the (command, flags, state, scope) safety matrix built on the advanced two-DC baseline, the consequence-axis rule behind the destroy data-loss predicate, and the token vocabulary bound to its published homes. | [apply-destroy-authorization-guards.md](apply-destroy-authorization-guards.md) |
| Planner gotchas: prior-phase capability injection silently dropping ordering deps, conditional ISO dependencies, virtctl provisioning/preflight split, phantom install records on skip, provisioning-playbook guards, ordering-vs-hard deps, and where apply-result state belongs. | [apply-task-graph-planning.md](apply-task-graph-planning.md) |
| Apply scheduler capacity: graph-full cluster-install and Redfish defaults, their explicit throttles and partial grants, immediate blocker publication, what the run-tree phase count means, why ISO creation is inside an install chain, and the parked-chain admission rule. | [apply-cluster-install-slot.md](apply-cluster-install-slot.md) |
| Substrate destroy refuse-foreign/warn gates stay per-resource: a shared foreign_gate helper was evaluated and rejected (non-uniform control flow, resource-specific fail_msg remediation, audited test pins). | [substrate-destroy-gate-no-shared-helper.md](substrate-destroy-gate-no-shared-helper.md) |
| How the container-cluster install-record gate refuses silent adoption, cross-path records, malformed writer evidence, and invalid status/phase pairs; why destroy must remove controller-side install state; how apply --mode rebuild skips healthy Available=True clusters but rebuilds on probe error; and the structural-hash migration-safety rules. | [cluster-install-record-gates.md](cluster-install-record-gates.md) |
| Plan tasks must hash a projection of the desired-state inputs they depend on (never the scope-filtered State), and base-only runs omit the agent-ISO dependency so they reuse a prior deps run's ISO instead of blocking. | [cluster-scoped-plan-stability.md](cluster-scoped-plan-stability.md) |
| Hashing and evidence invariants for convergence records and managed-OS markers: task-identity keying, scope-independent inputs, materialized secret paths reduced to stable identities, controller-side policy excluded from the hash, host-scoped fabric projection and frozen marshal shape, structural-hash exclusions, retaining archived-ok proof across a later graph failure, the retired storageAttachmentApply allowlist member, lenient-vs-strict record loading, and Ceph rebuild-authorization vars. | [converge-hash-drift-model.md](converge-hash-drift-model.md) |
| The unowned-teardown escape hatch is two tokens split by blast radius: --authorize unowned-vms covers per-VM markers, --authorize unowned-networks covers the libvirt network and KubeVirt DataVolumes/PersistentVolumeClaims, neither lifts the other, and guards stay presence-gated so a missing resource is a clean no-op. | [destroy-force-unowned-uniformity.md](destroy-force-unowned-uniformity.md) |
| Destroy gate extra-var vocabulary and its single composition point, the storage work-set tri-state, infra-component-before-machine teardown ordering, the hard machine-to-container edge, KubeVirt unreachable fail-closed behavior, root-gated record cleanup, live-classified orphan sweeps, non-OK authorized-unreachable bookkeeping, exact-owner binding for `storage destroy proof ... claims a clean no-op while an exact controller owner still exists`, shared-service release/block planning, and `--purge-history` scope. | [destroy-scoping-and-sweeps.md](destroy-scoping-and-sweeps.md) |
| Destroy graph input caching: the initial immutable full render alone owns storage assets; content-keyed task overlays emit inventory and vars; ownership snapshots advance by invalidated generations; context secrets live once per graph while KubeVirt kubeconfigs stay task-scoped; atomic counters distinguish cache work from runner launches. | [destroy-graph-run-input-cache.md](destroy-graph-run-input-cache.md) |
| infraComponents management accepts only external\|managed; `none` is a name sentinel, the future `reference` management value is unaccepted (do not author), and owner/reference are ownership-record roles — a separate concept. | [infra-component-management-vs-ownership-role.md](infra-component-management-vs-ownership-role.md) |
| Why day-2 node config re-applies every node Bootwright manages instead of pruning, why the task is planned for every cluster with nodes, why managed-fields (not existence) selects the relinquish set, the key+effect taint dedupe rule, the managed-by label that makes infra MachineConfigPool deletion safe, and why `--force-conflicts` is right here but not for add-ons. | [node-config-day2-reconcile.md](node-config-day2-reconcile.md) |
| Invariants of the per-context ownership store: owner/reference roles and filenames, cross-context exact identity and global serialization, durable host context claims, skip-not-fail loading, path allowlists, kind taxonomy, and package-removal gating. | [ownership-records-store.md](ownership-records-store.md) |
| How the serialized run lease, positive process-identity fencing, remote-host refusal, and takeover no-op semantics admit exactly one mutating run and self-heal after locally proven crashes without double-mutating. | [run-lease-mutual-exclusion.md](run-lease-mutual-exclusion.md) |
| How Satellite CA trust and registration are wired: day-2 katello bootstrap before subscription-manager register, kickstart anchors the CA in both %pre and %post, and Insights needs an explicit insights-client --register. | [satellite-registration-trust.md](satellite-registration-trust.md) |
| Quote(args) is display-oriented (denylist); QuoteWord/QuoteWords use a conservative allowlist and must be used for anything embedded in a generated command line that executes. | [shellquote-quote-vs-quoteword.md](shellquote-quote-vs-quoteword.md) |
| Why the trust store fails closed on divergent keys per address, why host-key capture uses ssh accept-new instead of ssh-keyscan under FIPS, and how ref-vs-managed known_hosts resolution and scope narrowing are centralized. | [ssh-trust-store-invariants.md](ssh-trust-store-invariants.md) |
| Per-substrate conclusive live ownership verification on apply, destroy, and orphan sweeps (libvirt XML, vSphere annotations/media records, exact KubeVirt VM/DV/PVC labels, provider-global BMC claims), why stale controller names never override foreign replacements or failed probes, rename-safe identity recording, shared DVD-cache exclusion, per-machine managed-OS destroy dispatch, cluster-scoped libvirt-network gating, and podman-label ownership for bastion containers. | [substrate-ownership-markers.md](substrate-ownership-markers.md) |
| The two-entry validate_certs:false allowlist policy and the SSL_CERT_FILE pattern for verifying downloads against a cluster's ingress CA instead of skipping TLS. | [tls-verification-allowlist.md](tls-verification-allowlist.md) |
| How the vCenter preflight session probe, session cleanup, 15s probe timeout, and the vCenter manual MAC-range validation work and why. | [vsphere-vcenter-integration.md](vsphere-vcenter-integration.md) |
| The minimum vCenter privilege set derived from the API call each module issues, which inventory object each grant goes on and whether it must propagate, why Config.Annotation is a correctness privilege, the leaf-name resolution trap, what is deliberately not granted, and why preflight can never prove the role. | [vcenter-minimum-privileges.md](vcenter-minimum-privileges.md) |
| Why a cloned vSphere machine is personalized with cloud-init through guestinfo/extraConfig rather than any vSphere guest-customization path, the networks[] IP-key invariant that keeps GOSC from racing cloud-init, the three independent facts that make a second apply a no-op, and what the seed may never carry. | [vsphere-guest-personalization.md](vsphere-guest-personalization.md) |
| community.vmware 6.x declares vcf-sdk while Bootwright pins pyvmomi and the preflight probes only `import pyVmomi`; harmless today, and why the fix would be to move the pin rather than widen the probe. | [community-vmware-vcf-sdk-divergence.md](community-vmware-vcf-sdk-divergence.md) |

### Ceph storage

| Topic | File |
| --- | --- |
| The cephadm bootstrap one-shot contract: what seeds into bootstrap ceph.conf, the unconditional/conditional bootstrap flags, the ceph.conf idempotence gate, dashboard-password capture, service-spec key placement rules, and the shared pre-mutation root-filesystem budget. | [ceph-cephadm-bootstrap-contract.md](ceph-cephadm-bootstrap-contract.md) |
| Why every `cephadm shell` argv needs its own finite class, the honest probe/config/orchestration/removal/tool/batch defaults, TERM-to-KILL escalation, rc 124/137 unknown-outcome handling, exact mutating retry fact, read-only command-free diagnostic, and recursive closed-surface guard. | [ansible-ssh-liveness-timeouts.md](ansible-ssh-liveness-timeouts.md) |
| The facet model behind Ceph `bootwright diff` (live is the default; `--adopt` implies it): which live objects are filtered as internal, the OSD-device comparison rules and reconstruction advisories, absent-read semantics, and the additive-only adopt policy with its refusals. | [ceph-diff-adopt-live-semantics.md](ceph-diff-adopt-live-semantics.md) |
| Distribution-table-driven Ceph packaging and its traps: authored releases and subscription image bases, no release/RHEL compatibility catalog or compiled OS-stream suffix, provider artifact policy plus capability-named native preparation/parity adapters, independent IBM Storage and Cephadm Ansible package coordinates, exact preinstall and post-preflight RPM verification with no downgrades, RPM-owned preflight `present`/`latest` behavior and ABI isolation, repository-origin suppression, fresh report/cache/temp containment, image-base/mirror coherence, floating daemon images, fail-closed package-mode residue and live FIPS gates (including IBM FIPS-build access), owned `sos`, IBM license/Call Home, mgr registry credentials, community Ceph/RHEL OpenSSL conflicts, EPEL bootstrap, RHSM idempotence, and authenticated ODFSI pairing checks. | [ceph-distribution-packaging.md](ceph-distribution-packaging.md) |
| Why an OSD readiness or mon-location poll never satisfies against a healthy cluster: cephadm hosts are FQDNs but CRUSH host buckets and mon/crash daemon names are the short hostname, so orchestrator names must not be compared with `ceph osd tree` / `ceph mon dump` output. | [ceph-host-identity-namespaces.md](ceph-host-identity-namespaces.md) |
| cephadm monitoring-spec constraints: retention keys are Prometheus-only, no dashboard Loki wiring command exists, role-less services render only when authored, the feature-derived read-only sidecar-pin advisory, the mgmt-gateway/keepalive_only ingress spec shape, why mgmt-gateway daemons never start on mgr hosts unless the classic dashboard's SSL port is disabled first, why RGW/NFS ingress inline TLS is the `ssl_cert`+`ssl_key` pair with `ssl: true` (a combined bundle leaves haproxy.pem unwritten on certmgr builds), how the authored `mgmt-gateway-spec-dependency-recording` workaround selects the safe HTTP shape without a vendor/release branch, why HTTP pins `ssl: false` and re-persists it past the falsy-dropping spec store, and why storage nodes open 29443/tcp for the gateway's internal monitoring endpoint. | [ceph-monitoring-service-specs.md](ceph-monitoring-service-specs.md) |
| Why a fresh multi-host install can leave a stray, unmanaged mon: cephadm's default unrestricted mon/mgr placement races the declared narrow placement across the host-add/OSD-reclaim gap, and how the render layer closes that window. | [ceph-mon-mgr-default-placement-race.md](ceph-mon-mgr-default-placement-race.md) |
| Why a single Ceph node can start no container at all: kernel lockdown from EFI Secure Boot refuses the eBPF device filter crun installs through systemd, the probe that discriminates it from SELinux/cgroup/EDR causes, the pre-bootstrap gate that fails closed naming the host, and the device-isolation trade the cgroupfs drop-in accepts. | [ceph-node-container-runtime-lockdown.md](ceph-node-container-runtime-lockdown.md) |
| Why cephadm silently drops a declared mon host: the `public_network` overlap filter on mon placement, why a stretch arbiter on a subnet of its own needs a second `publicCIDRs` entry rather than a widened one, how that differs from the stricter exact-subnet rule `cephadm bootstrap` applies to the bootstrap node, and why the networks must be asserted before the first `ceph orch apply` for a corrected declaration to converge. | [ceph-mon-public-network-placement.md](ceph-mon-public-network-placement.md) |
| Non-root Ceph node access traps: changing `Machine.spec.access.ssh.user` makes probe_existing.yml read the node as not-installed, which now fails closed on the unverifiable refusal (it silently reinstalled before 2026-07-23), the load-bearing two-space indent in the cephadm ssh_config `User` line that `ceph cephadm set-user` rewrites by regex, and why cephadm forces `NOPASSWD: ALL` (its mgr sudo-wraps every remote command when ssh_user != root). | [ceph-nonroot-node-access.md](ceph-nonroot-node-access.md) |
| The storage_node_access privileged channel, section by section: terminal detection (differential pty-free/`-tt` probe, never a string match), the borrow-vs-create asymmetry, what a pty does to the bytes, the three ways a correct sudoers drop-in is never evaluated, and the install-vs-cluster restore key. | [ceph-node-access-privileged-channel.md](ceph-node-access-privileged-channel.md) |
| The OSD device ownership marker and exact-path install/destroy/removal gates; dynamic data/DB/WAL filter validation and the read-only pre-service emptiness gate; why rebuild/data-loss never bypasses narrowing selection and only an effective unbounded managed data `all` may auto-reclaim; the managed-OS reinstall reclaim path, absent/unprobeable/mounted split, orphan-vs-live-OSD holders branch (ADR 0034), root-disk validators, readiness poll, and exhausted-retry diagnostics. | [ceph-osd-device-safety.md](ceph-osd-device-safety.md) |
| What counts as a Ceph sub-object's immutable structural identity, the fail-safe live probes that gate the data-destroying --mode rebuild rebuild, EC pool creation traps, and the structural-hash invariants for reconcilable edits. | [ceph-override-structural-rebuild.md](ceph-override-structural-rebuild.md) |
| How Bootwright proves a cephadm cluster is its own before any destructive action: the 3-factor ownership gate, exact declared-mon fallback when the seed is dead, FSID-confirmed marker recovery, on-disk-fsid semantics, per-host rm-cluster, pre-bootstrap ownership recording, the fail-safe rebuild-authorization token, and the per-node foreign-cluster gate with the `foreign-daemons` token that clears it in-product (ADR 0038). | [ceph-ownership-apply-destroy-gates.md](ceph-ownership-apply-destroy-gates.md) |
| RGW realm/zonegroup/zone creation and period-commit ordering ahead of the rgw service, admin-user output redaction, and declarative NFS export upserts ordered after the NFS service is ready. | [ceph-rgw-nfs-service-ordering.md](ceph-rgw-nfs-service-ordering.md) |
| Why `ceph orch apply` returning and the cluster reporting HEALTH_OK do not mean the declared mds/rgw/ingress/monitoring services are deployed, and the post-apply service-readiness gate that closes it. | [ceph-service-rollout-gate.md](ceph-service-rollout-gate.md) |
| Every precondition `ceph mon set_new_tiebreaker` enforces, why the mon service spec (not the host spec) carries the tiebreaker's CRUSH location, the add-before-retire order that keeps quorum intact through a failure, and the three disaster variants (host gone, quorum degraded, no third site). Also why the no-op predicate is retirement residue rather than `tiebreaker_mon`, why the mon daemon name and the cephadm host name must stay apart, why the plan is recomputed after the input rewrite, and why the converge-record refresh is bounded to what the run converged. | [ceph-arbiter-replacement.md](ceph-arbiter-replacement.md) |
| Why the stretch tiebreaker must not get a CRUSH host-spec location, how the two-replicas-per-site rule is compiled into the CRUSH map, the ordering/naming constraints around enable_stretch_mode, why every declared mon (not just the tiebreaker) must be in the monmap first, and the internal-pool re-homing an arbiter-less cluster never gets from it. | [ceph-stretch-mode-constraints.md](ceph-stretch-mode-constraints.md) |

### Bare-metal & managed-OS install

| Topic | File |
| --- | --- |
| Jinja/kickstart interactions: trim_blocks newline loss requires precomputed flag expressions, credential '/' must be %2F-encoded, proxy secrets are shape-asserted via undef(), and proxy bypass is decided per kickstart directive at render time. | [anaconda-kickstart-render-traps.md](anaconda-kickstart-render-traps.md) |
| The guards that keep a first bare-metal apply from wiping the wrong host: confirm-time first-install warning, fail-open Redfish occupancy probe, normalized unique-BMC-address validation, authenticated BMC reachability preflight, and the record-keeping-only bare-metal substrate role. | [baremetal-first-install-safety.md](baremetal-first-install-safety.md) |
| The libvirt media insert helper must register the bootwright XML namespace prefix to keep the ownership marker intact across virsh define, stages itself at a content-hashed path to avoid concurrent-version races, and the eject script's rc-capture and source/all mode contract. | [libvirt-direct-media-helper.md](libvirt-direct-media-helper.md) |
| Why each of the three TPM-bound encryption paths uses a different binder (clevis in `%post`, Ignition in the initramfs, `systemd-cryptenroll` on the Ceph host), the `%post --erroronfail`/marker-ordering/dracut rules, unconditional retained-Kickstart cleanup, what an omitted `pcr_ids` actually seals, why OpenShift renders no cipher option and pins Ignition 3.2.0, how the positive-only Redfish `TrustedModules` preflight closes the extra-manifest gap, the `tpm2-tss` weak-dependency trap and IBM 9.9 support boundary behind `osd.tpm2`, and how emulated TPMs lose state. | [disk-encryption-tpm.md](disk-encryption-tpm.md) |
| The probe_existing/wait/marker predicate ladder: port-open detection, fail-closed unverifiable hosts, ownership vs hash-match separation, the force_rebuild/install_required fact chain that makes --mode rebuild actually reinstall, install_required-gated post-boot waits, ownership-gated stamp gating against foreign-host laundering, bare-metal media eject via cleanupMediaRole, the infra-stage destroy-protection remedy routing, and why `customizations.storage.wipe` was removed rather than wired up. | [managed-os-install-gates.md](managed-os-install-gates.md) |
| Metadata-only media inventory and opt-in checksum cost, shared-source staging identity and throttles for controller-collapsed installs, hosted-install-tree identity/atomic-publish rules, why rd.live.check is stripped from mkksiso builds, and publish-root SELinux label alignment (chcon --reference, never restorecon). | [managed-os-media-staging.md](managed-os-media-staging.md) |
| What renders into the minimal kickstart (one merged bond+VLAN stanza) vs the post-install desiredState (all VLANs, MTU, cluster IPs), why RHEL 9 rejects split bond/VLAN directives and applies a merged-line MTU only to the child, why bond members carry mac-address identity while bond/VLAN layers must not, and the controller-local inventory shape bare-metal managed-OS nodes need. | [managed-os-post-install-network.md](managed-os-post-install-network.md) |
| Trust-store method discovery/dispatch, iBMC RootCertId slot rules, openssl s_client leaf retrieval, no_log-safe refusal surfacing, capture-once VerifyCertificate, and always-block cleanup/restore semantics for BMC virtual-media cert trust. | [redfish-bmc-cert-import-mechanics.md](redfish-bmc-cert-import-mechanics.md) |
| Role-wide uri module_defaults, tail-recursive retry/power-wait pattern (never `until` under no_log), sushy cold-init lock throttle, dual VirtualMedia view dedup, multi-system pinning via /Systems/<id> suffix, and emulated-BMC service provisioning gotchas. | [redfish-boot-flow-quirks.md](redfish-boot-flow-quirks.md) |

### OpenShift / OKD clusters

| Topic | File |
| --- | --- |
| The mirror registry host prefers a declared endpoint or routable bind address over the machine's SSH alias, and the installer manifest render must recognize both secret-placeholder dialects so redacted cert/key material lands verbatim in stringData instead of being base64-corrupted. | [openshift-installer-render-resolution.md](openshift-installer-render-resolution.md) |
| vSphere boot always full power-cycles, probes readiness over SSH (agent ISOs have no VMware Tools), omits integrated-LB VIPs when endpoints are externally load-balanced, masks manual MACs into vCenter's 00:50:56 range, and fabricates substrate NICs only for physical interfaces. | [openshift-vsphere-agent-boot.md](openshift-vsphere-agent-boot.md) |
| Invariants for the nested-cluster child network add-on: L2-only CUDN with static IPs, bridge-mapping name equality, VLAN plumbing, and the OVN-derived NAD the VMs consume. | [ovn-child-network-wiring.md](ovn-child-network-wiring.md) |

### Networking, proxy & TLS

| Topic | File |
| --- | --- |
| The machine_proxy role's persistence rules: env-only dnf proxy, strip-only config edits, no containers.conf drop-in, TCP reachability probe, and active clearing of stale proxy state. | [machine-proxy-persistence.md](machine-proxy-persistence.md) |
| Fallback chain that resolves the cluster-reachable host for managed proxy and mirror-registry URLs, including gateway substitution for loopback bastions. | [managed-proxy-url-resolution.md](managed-proxy-url-resolution.md) |
| The rollback-guarded post-install nmstate apply design, the interfaceAddresses family-default gotcha, and why only physical NMState interfaces get substrate NICs/MACs. | [nmstate-network-convergence.md](nmstate-network-convergence.md) |
| The Bypasses matching contract, why CIDR noProxy entries are expanded into pinned literals over noProxyTargets, and the CIDR-stripped literal variant for CIDR-blind matchers. | [no-proxy-cidr-matching.md](no-proxy-cidr-matching.md) |

### Secrets & entitlements

| Topic | File |
| --- | --- |
| entitlements.Resolve keeps secrets behind names and derives provider/product from spec.type to preserve the pre-first-class render contract; rhsm resolves from the entitlement's own arm (ibm-storage-ceph carries none; its nodes register via a separate redhat-rhel subscription named by the profile subscription or cluster osSubscriptionRef); Satellite projects into vars with a stable CA path. | [entitlement-resolution-vars.md](entitlement-resolution-vars.md) |
| How plaintext secret copies on disk are kept short-lived: post-lease sweep reclaims task-runtime SIGKILL orphans, store materialization is all-or-nothing, single-material callbacks stay under private runtime parents, cluster capture performs exact-key post-capture encryption around the nested add-ons area, MkdirAll needs explicit Chmod, and preflight rejects only over-broad modes. | [plaintext-secret-lifecycle.md](plaintext-secret-lifecycle.md) |
| The NUL-wrapped placeholder sentinel switches resolution to {{ secret }} tokens (render-only, short-circuits before source paths leak); portable bundles are leak-scanned; manifest Secrets must handle both placeholder dialects in stringData. | [portable-secret-placeholders.md](portable-secret-placeholders.md) |
| secret.Index is the single seam from Secret declarations to per-role paths; materialize verifies-or-renews generated material via the same request derivation preflight uses; SEC1 note for ECDSA SSH keys. | [secret-index-resolution.md](secret-index-resolution.md) |
| clusteraccess summaries report paths/presence only with RevealClusterSecret gated behind --secrets; the global pull-secret merge is conflict-safe via resourceVersion, idempotent on existing credentials, and console-silent. | [secret-output-discipline.md](secret-output-discipline.md) |

### Add-ons & OLM

| Topic | File |
| --- | --- |
| How the add-on engine's already-ready short-circuit, fail-closed Ready CSV evidence refresh, audit-only recorded/live CSV reporting, OLM catalog/CSV gates, typed gate-timeout errors, quiet poll runner, and stage-scoped preflight gates behave and why. | [addon-apply-engine-gates.md](addon-apply-engine-gates.md) |
| Why the Data Foundation external-cluster attach breaks in two independent ways: a meta-operator CSV that establishes no CRD, and an IBM Storage Ceph 20.2.1 cephx key type the bundled Data Foundation client cannot parse. | [data-foundation-external-attach.md](data-foundation-external-attach.md) |
| IBM Fusion Data Foundation's catalog registry pod requires an explicit restricted security context; the typed CatalogSource pod-config field and native add-on pin prevent the CatalogSource readiness gate from blocking the Subscription. | [fusion-catalog-security-context.md](fusion-catalog-security-context.md) |
| provides/requires capabilities are an open token vocabulary with three reserved planner names (kubevirt, dataFoundation, nmstate); ordering is a stable per-binding Kahn topo sort where unsatisfied requirements impose no edge. | [addon-capability-ordering.md](addon-capability-ordering.md) |
| render.DesiredHash folds the canonical OLM resource order, binding-supplied inputs (omitempty, duplicate-merged to mirror the executor), and the per-step shipped-content digest — with the parity invariants that keep hash and execution aligned. | [addon-desired-hash-inputs.md](addon-desired-hash-inputs.md) |
| Step execution internals: desired content+inputs+target digest for onChange, observed runtime sha256 evidence, output-set atomic validation, ad-hoc SSH inventory with scoped secrets and the Ceph post-install identity, one task-scoped materialized kubeconfig, fixed output paths, fail-closed firstReachable classification and storage-target resolution, step-level `storage:<name>` mutation serialization, exportDetails resolution, and cross-cluster DAG edges. | [addon-step-runtime-internals.md](addon-step-runtime-internals.md) |
| Machine-local add-on store internals: extensionless provenance marker, whole-directory rewrite on install with path-escape rejection, version-as-assertion delete, SourcePath-based store detection, the context-init snapshot that keeps contexts self-contained, and why upgrading Bootwright refreshes NEITHER the store nor the snapshot — a merged playbook fix runs nowhere until `add-ons add` and `context update` are both re-run, which validation now refuses to let pass silently. | [addon-native-store-internals.md](addon-native-store-internals.md) |
| Observed-state add-on records never hold secret bytes (non-secret failure summaries and runtime digests, raw oc output only in the error/apply log), successful output sets validate atomically before persistence, SaveRecord preserves on-disk Steps written out-of-band by SetStep, and step secret materialization is name-scoped. | [addon-records-secret-hygiene.md](addon-records-secret-hygiene.md) |
| Step manifest tokens must be a whole YAML scalar {{ kind arg }}; interpolation is rejected so re-marshaled payloads cannot be corrupted or injected. | [step-manifest-token-grammar.md](step-manifest-token-grammar.md) |

### CLI behavior

| Topic | File |
| --- | --- |
| Cluster rsh/exec authenticates as the cluster's orchestration identity (nodeSSH + core, or clusterSSH user + key) while machine rsh/exec stays the Machine's own login; why a login must never be resolved without its credential; encrypted private keys cross the SSH boundary through parent-held anonymous descriptors and child exit codes are preserved. | [cluster-node-ssh-access.md](cluster-node-ssh-access.md) |
| Failure-presentation rules: per-cluster log pointers with installer paths only on unclean finishes, pre-run failures materialize advertised task logs, blocked tasks are attributed to the first FAILED ancestor (naming the host cluster), and middle-ellipsis truncation preserves the trailing remedy clause. | [cli-apply-failure-reporting.md](cli-apply-failure-reporting.md) |
| Single-source rules for shared flag help and the --stage/--through vocabulary, the --mode/--authorize vocabulary contract, cobra dispatcher wiring gotchas, the -- flag-terminator rule for JSON error output, and the unbuffered confirm() stdin gotcha. | [cli-flag-and-parsing-conventions.md](cli-flag-and-parsing-conventions.md) |
| Operational mechanics of the sudo re-exec: --context stripping for classification, cutting the forwarded payload (-- terminator, passthrough operands) before any argv scan, the command-table drift guard, workspace-path resolution before escalation, the internal password-handoff env vars, signal/Ctrl-C ownership, and the never-escalate/never-prompt rules for completion and preflight. | [cli-root-gate-sudo-reexec.md](cli-root-gate-sudo-reexec.md) |
| How apply/destroy ledgers project into display frames: the single ledger-to-frame mapper, the leading infra group, topology-aware cluster ordering, the single-group destroy frame, the two storage-cluster phases, and the internal/status vs internal/cli and mutating_run.go ownership boundaries. | [cli-run-frames-and-grouping.md](cli-run-frames-and-grouping.md) |
| The status next-step spine: explicit context and unanswered confirmation on normal structured hints, immutable original argv for audit plus typed CLI-resolved recovery steps, command-free legacy-ledger fallback, identity-vs-health rendering, when diff joins the spine, exhaustive secret-set hints, the machine-trust hint, and access advertised only for completed installs. | [cli-status-and-next-step-hints.md](cli-status-and-next-step-hints.md) |
| The output package contract: one color style gate, RunView redraw vs append-only modes, RenderFrame collapse and the single-frame-writer allowlist, redraw height accounting, the status --watch preamble, and the quiet oc ReadRunner that keeps expected read noise off the console. | [cli-terminal-rendering-contract.md](cli-terminal-rendering-contract.md) |
| Where diagnostic presentation lives (CLI, not validator), the structured Finding carrier, the append-only decode did-you-mean rule, defaulted-secret-ref notes, and the rule that scaffolds must not imply apply support. | [cli-validation-diagnostics.md](cli-validation-diagnostics.md) |

### Rendering & templates

| Topic | File |
| --- | --- |
| Rendered-only spellings and boundary vocabularies: image.mediaType, machines/bmc service kinds, desired `artifactServer`/`loadBalancer` versus durable `artifacts`/`load-balancer`, historical HTTP naming of the 8443 HTTPS port, verbatim upstream keys, and the (C)UDN multus wiring rule. | [render-internal-vocabularies.md](render-internal-vocabularies.md) |
| Inventory role groups (ADR-0002), controller-driven-on-localhost, substrate-blind boot_redfish, emulated-BMC port projection, vSphere path/MAC/ISO reduction, and cleanupMediaRole dispatch. | [render-inventory-and-boot-vars.md](render-inventory-and-boot-vars.md) |
| Two keep-in-sync hazards in render: the duplicate-BMC key in state/desired hand-mirrors the renderer's Redfish URL normalization, and componentImages overrides must bypass managedServiceImage to avoid infinite recursion while still surfacing in the BOM. | [render-normalization-mirrors.md](render-normalization-mirrors.md) |
| renderCore's shared body and four mode seams, effective-state identity, the fail-closed unresolved-name check, and the omit-empty-keys byte-stability rule. | [render-pipeline-modes.md](render-pipeline-modes.md) |
| StorageRenderState nils ContainerClusters and does not follow the data-foundation edge; top-level render --clusters accepts StorageCluster names. | [render-storage-scope.md](render-storage-scope.md) |
| Render-reference vs work-set semantics for --clusters scopes: nil-vs-empty StorageClusterNames, preflight secret scope, connected-machine host trust, diff --recorded parity, full-state orphan detection, whole-input validation, service-machine retention. | [scoped-runs-render-vs-work-set.md](scoped-runs-render-vs-work-set.md) |

### API schema & grammar

| Topic | File |
| --- | --- |
| Never-authored computed fields (DefaultedRefs, binding MachineName) that let validation blame normalize-injected references honestly, plus the StorageCephRoles/StorageCephRHELVersions single-source accessors that keep advertised and accepted sets from drifting. | [api-normalize-bookkeeping.md](api-normalize-bookkeeping.md) |
| Lookup table of every *Ref field's resolution namespace that lived only in deleted field comments; specs/state-model.md and docs/concepts/ remain the normative home. | [api-ref-resolution-namespaces.md](api-ref-resolution-namespaces.md) |

### Repo tooling, build & CI

| Topic | File |
| --- | --- |
| Maps the state-engine audit-finding IDs (M2 lease takeover, M8 scope-dependent hash, M11 diff --recorded --clusters parity) to the invariants they encode and the regression tests that pin them. | [audit-finding-regression-tests.md](audit-finding-regression-tests.md) |
| Containerfile layer ordering (toolchain COPY, requirements-keyed galaxy layer, second sync-bundle, compile-only relink, .git last) and the VERSION/ldflags stamping chain plus the CGO-off shipped-binary constraint. | [container-build-and-stamping.md](container-build-and-stamping.md) |
| How to regenerate render goldens and Ansible-UUID pin values, and why goldens stay byte-stable (pinned HOME, fictitious secrets dir, static lookupDate freshness stamps). | [golden-fixture-regeneration.md](golden-fixture-regeneration.md) |
| The check tiers (scoped/feature/fast/full, ADR 0041), why the targets run in their order (sync-bundle first, cheapest-to-slowest), per-target rationale (race timeout, tidy trap, shebang shellcheck, workflow yamllint, repo-local Ansible temp) and the CI trigger gates on tags, PRs, and docs publishes. | [make-check-and-ci-gates.md](make-check-and-ci-gates.md) |
| The intent behind the converge facade, workflow purity rules, state package layering (view/graph/advice), inventory-installer edge usage, and the frozen test-only compat aliases in internal/cli. | [package-boundary-contracts.md](package-boundary-contracts.md) |
| Checklists for coordinated edits guarded by coverage tests: new kinds, InfraComponent union arms, installer-owned fields, provisioning stages, machine-service var builders and pin gates, native add-on catalog embeds, scaffold substrates, and the credentials filter contracts. | [registry-coverage-guards.md](registry-coverage-guards.md) |
| Catalog of the repository fitness tests (import matrix, process-exec seam, role-name single source, human-output guard, file-size floors, stale-term scope, diagnostics vocabulary, docs YAML strict decode, docs links inside docs_dir, get_url checksum policy) and what each forbids. | [repo-fitness-guardrails.md](repo-fitness-guardrails.md) |
| Gitignored work-in-progress fixtures live under examples-wip/ (a parameterized template plus render.sh → rendered/, a sibling of examples/ so example-scoped checks skip them); this file is the only in-repo record they exist. | [examples-wip-fixtures.md](examples-wip-fixtures.md) |

### Ansible runtime quirks

| Topic | File |
| --- | --- |
| Negative pin: Bash length expansion ${#...} is forbidden in eject_libvirt_media.sh (and Ansible-templated shell) because {# opens a Jinja comment; guard test enforces, count with a loop variable instead. | [ansible-bash-hash-brace-jinja.md](ansible-bash-hash-brace-jinja.md) |
| Task-playbook preambles cannot be factored (play keywords); every play the planner can fan out over more than one machine uses `strategy: linear` so one TASK banner covers every machine, `free` and `host_pinned` are retired repo-wide and pinned by TestNoPlaybookReintroducesTheFreeStrategy, plus the remaining deliberate variants (bastion env lookup, reachability no-become, managed-OS any_errors_fatal, storage linear for hostvars lockstep, machine-destroy three-play barriers). | [ansible-playbook-preamble-variants.md](ansible-playbook-preamble-variants.md) |
| Task output is grouped, but a mixed-substrate cluster boots one substrate after another: a per-host templated `include_role` NAME splits the linear play into lockstep buckets that run in sequence (a templated argument does not split). Negative pin: `throttle` binds under `free` too, so retiring `free` activated nothing. | [ansible-linear-lockstep-include-buckets.md](ansible-linear-lockstep-include-buckets.md) |
| The embedded bundle pipeline: exact-pinned collections, tests/docs slimming before pack, two-layer lock verification (install-time script + post-pack Go test), and byte-deterministic zip with fail-closed symlink policy. | [ansible-bundle-embed-and-lock.md](ansible-bundle-embed-and-lock.md) |
| The repo-enforced no_log template forms, the agent-ISO publish token as a secret, Redfish module_defaults credential wiring, the tolerate-then-assert pattern for BMC errors, the shared credential loader contract, why the public-by-contract cloned-guest seed carries no no_log, and render-time kickstart proxy credential handling. | [ansible-credential-redaction.md](ansible-credential-redaction.md) |
| Plugin unit tests live under semantic collection test siblings because runtime plugin directories import every Python file; Makefile explicitly discovers filter, callback, and module siblings because unittest does not recurse through non-package directories. | [ansible-filter-test-placement.md](ansible-filter-test-placement.md) |
| check_external_reachability probes only what Bootwright itself drives (external Redfish BMCs, external proxies via TCP-only https-first), skips components the same apply will create, deliberately leaves operator DNS/LB to later controller-vantage gates, and keeps a per-BMC probe task (not a loop) because no_log censors a loop item label and would hide which BMC failed. | [ansible-infra-reachability-preflight.md](ansible-infra-reachability-preflight.md) |
| Rationale for every .ansible-lint skip and .yamllint relaxation, plus how make ansible-lint-check invokes both tools; do not remove a skip without addressing its reason. | [ansible-lint-yamllint-config.md](ansible-lint-yamllint-config.md) |
| Four Ansible result-handling semantics: skipped set_fact keeps old values, branch-only registers persist across loops, skipped tasks overwrite registers, and failed_when:false forces decisions onto success-only payload keys. | [ansible-register-skip-persistence.md](ansible-register-skip-persistence.md) |
| Play mechanics that let a destructive teardown classify unreachable hosts and fail closed with guidance: defer fact gathering, probe with task-level ignore_unreachable, gate with controller-side asserts under any_errors_fatal. | [ansible-teardown-unreachable-hosts.md](ansible-teardown-unreachable-hosts.md) |
| Agent teardown is entirely controller-side; every cluster node must be installer-OS-managed and the common boot action rechecks that invariant before provider dispatch; media cleanup dispatches on the rendered cleanupMediaRole registry; FIPS clusters build their ISO with openshift-install-fips; and external endpoint DNS diagnostics are report-only with no end_play under strategy:free. | [container-cluster-ansible-flow.md](container-cluster-ansible-flow.md) |
| from_json on a tolerated command's empty stdout throws (an rc!=0 task with stdout='' is DEFINED, not skipped); guard with default('{}', true) before from_json, or an eager fail_msg reference throws first. | [ansible-from-json-empty-stdout.md](ansible-from-json-empty-stdout.md) |
| Deployed Jinja has no list/dict comprehension or multi-for grammar; build derived collections with filter chains (map/select/reject) and accumulator set_facts instead. | [ansible-jinja-no-comprehensions.md](ansible-jinja-no-comprehensions.md) |
| YAML unescapes only in double-quoted scalars and Jinja never unescapes string literals, so in a folded (`>-`) scalar `join('\n')` joins with a literal backslash-n and every stdout comparison against it is permanently False (measured length 6 vs 5 on ansible-core 2.20.4); `replace('\r','')` is correspondingly inert when folded and strips newlines rather than CRs when double-quoted. | [ansible-folded-scalar-escapes.md](ansible-folded-scalar-escapes.md) |
| A host-wide finalizer refuses its freshly acquired canonical guard as unreadable or malformed, `jq` reports `Invalid numeric literal at EOF`, or `sed -n l` shows JSON ending in `}\\n$`; a folded Jinja newline expression wrote literal bytes `5c 6e`, so deleting the guard only recreates the failure. | [ansible-folded-scalar-escapes.md](ansible-folded-scalar-escapes.md) |

### Workspace & context

| Topic | File |
| --- | --- |
| context init copies input; update snapshots to history via the single input-mutation component; crash-safe replace; paths resolve before sudo re-exec; missing input is a named error. | [context-input-ownership.md](context-input-ownership.md) |
| Root-owned workspace artifacts need CommandOutputLocalRoot; operator files need caller-owned stat; EnsureDir re-chmods; copyInputTree is symlink-safe. | [workspace-root-owned-fs-access.md](workspace-root-owned-fs-access.md) |

## Backlog

Deferred and open engineering work is cataloged in [BACKLOG.md](BACKLOG.md), not in this index. Before proposing new work in an area, scan it for an existing entry; before deferring a review finding, file one there. Each entry is deleted in the same commit that lands its fix — git history is the archive — or is converted into a rejected-note in the owning knowledge file when the work is declined. Keeping it the single home for intentionally not-yet-done work lets this index stay a record of durable truths rather than a to-do list.
