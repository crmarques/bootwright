apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: baremetal-redfish-fleet
spec:
  baseDomain: bootwright.test

  resources:
    - secrets.yaml
    - infra
    - clusters

  infraComponents:
    artifactServers:
      - name: default
        management: managed
        componentRef: artifact-server
