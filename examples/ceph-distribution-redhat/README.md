This example shows Red Hat Ceph Storage distribution selection through a generic
Environment entitlement. The secret names are declarations only; secret material
is supplied outside versioned content.
