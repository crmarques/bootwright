apiVersion: bootwright.io/v1alpha1
kind: InfraComponent
metadata:
  name: artifact-server
spec:
  artifactServer:
    hostRef:
      name: bastion
    endpoints:
      - name: bmc
        listener: https
        hostAddress: lab-lan
      - name: cluster
        listener: https
        hostAddress: lab-lan

