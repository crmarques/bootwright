apiVersion: bootwright.io/v1alpha1
kind: ClusterAddon
metadata:
  name: cluster-observability
spec:
  type: manifestSet

  manifestSet:
    manifests:
      - path: manifests/observability-namespace.yaml

  readiness:
    checks:
      - resourceExists:
          apiVersion: v1
          kind: Namespace
          name: observability
