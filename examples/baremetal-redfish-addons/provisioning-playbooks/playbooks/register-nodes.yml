---
- name: Register nodes with the CMDB
  hosts: all
  gather_facts: false
  tasks:
    - name: Announce the registration target
      ansible.builtin.debug:
        msg: "would register {{ inventory_hostname }} with {{ cmdb_url | default('the CMDB') }}"
