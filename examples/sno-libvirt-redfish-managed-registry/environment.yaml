apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: sno-libvirt
spec:
  baseDomain: example.test

  resources:
    - shared/hosts.yaml
    - shared/networks.yaml
    - shared/provider.yaml
    - shared/infra-component.yaml
    - clusters/sno-libvirt/cluster-infra.yaml
    - clusters/sno-libvirt/container-cluster.yaml

  secrets:
    openshift-pull-secret: null
    cluster-admin-ssh-key:
      generated:
        sshKeyPair:
          type: ed25519
          comment: bootwright-cluster-admin
    provider-host-ssh:
      file: ~/.ssh/bootwright-ssh-key
    bmc-credentials:
      generated:
        credentials:
          username: admin
    mirror-registry-credentials: null
    mirror-registry-ca:
      file: ./mirror-registry-ca.crt

  registries:
    mirror:
      credentialsRef:
        name: mirror-registry-credentials
      trustBundleRef:
        name: mirror-registry-ca

  infraComponents:
    artifactServers:
      - name: default
        type: managed
        default: true
        componentRef:
          name: artifact-server
        routes:
          clusterInstall:
            endpoint: cluster
    nameResolution:
      - name: default
        default: true
        type: managed
        componentRef:
          name: name-resolution
    registries:
      - name: default
        default: true
        type: managed
        componentRef:
          name: registry
