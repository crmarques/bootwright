apiVersion: bootwright.io/v1alpha1
kind: InfraComponent
metadata:
  name: artifact-server
spec:
  artifactServer:
    hostRef:
      name: lab-host
    listeners:
      - name: https
        protocol: https
        port: 8443
    endpoints:
      - name: cluster
        listener: https
        addressName: cluster-lan
