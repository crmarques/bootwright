apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: rgw
spec:
  storageClusterRef: ceph-ibm

  placementPolicyRef: replicated-host

  ceph:
    # Object storage (RGW) data pool.
    role: rgw
