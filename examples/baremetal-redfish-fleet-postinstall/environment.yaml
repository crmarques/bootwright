apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: demo-baremetal-fleet-postinstall
spec:
  baseDomain: example.test

  resources:
    - shared/hosts.yaml
    - shared/networks.yaml
    - shared/provider.yaml
    - shared/infra-component.yaml
    - demo-ocp-a/cluster-infra.yaml
    - demo-ocp-a/container-cluster.yaml
    - demo-ocp-b/cluster-infra.yaml
    - demo-ocp-b/container-cluster.yaml
    - extensions/cluster-extension-virtualization.yaml
    - extensions/cluster-extension-gitops.yaml
    - extensions/cluster-extension-set.yaml
    - cluster-extension-binding.yaml

  containerClusters:
    - demo-ocp-a
    - demo-ocp-b

  secrets:
    openshift-pull-secret: null
    cluster-admin-ssh-key:
      generated:
        sshKeyPair:
          type: ed25519
          comment: bootwright-cluster-admin
    provider-host-ssh:
      file: ~/.ssh/bootwright-ssh-key
    bmc-credentials:
      generated:
        credentials:
          username: admin

  infraComponents:
    artifactServers:
      - name: default
        type: managed
        default: true
        componentRef:
          name: artifact-server
        routes:
          redfishVirtualMedia:
            endpoint: bmc
          clusterInstall:
            endpoint: cluster
