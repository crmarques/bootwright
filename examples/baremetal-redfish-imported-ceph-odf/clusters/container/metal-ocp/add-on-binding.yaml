apiVersion: bootwright.io/v1alpha1
kind: ClusterAddonBinding
metadata:
  name: metal-ocp-odf
spec:
  clusterRef:
    name: metal-ocp

  addons:
    - name: openshift-data-foundation

  storage:
    - name: ceph
      exportRef:
        name: imported-ceph-odf
      dataFoundation:
        externalDetailsRef:
          name: imported-ceph-odf-details
