apiVersion: bootwright.io/v1alpha1
kind: InfraComponent
metadata:
  name: artifact-server
spec:
  type: artifactServer

  artifactServer:
    endpoints:
    - name: bmc
      listener: https
      machineAddress: lab-lan
    - name: cluster
      listener: https
      machineAddress: lab-lan
    machineRef:
      name: bastion
