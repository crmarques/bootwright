apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: odf-cephfs-metadata
spec:
  storageClusterRef:
    name: ceph-storage

  placementPolicyRef:
    name: stretch-replicated

  ceph:
    role: cephfs-metadata
    replicated:
      size: 4
      minSize: 2
