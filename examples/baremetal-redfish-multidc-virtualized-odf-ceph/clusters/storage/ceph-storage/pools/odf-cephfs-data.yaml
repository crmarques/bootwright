apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: odf-cephfs-data
spec:
  storageClusterRef:
    name: ceph-storage

  placementPolicyRef:
    name: stretch-replicated

  ceph:
    type: replicated # default
    role: cephfs-data
    replicated:
      size: 4
      minSize: 2
