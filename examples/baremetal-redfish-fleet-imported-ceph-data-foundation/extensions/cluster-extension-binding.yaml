apiVersion: bootwright.io/v1alpha1
kind: ClusterExtensionBinding
metadata:
  name: data-foundation-operator
spec:
  clusterSelector:
    names:
      - dc1-ocp
      - dc2-ocp

  applyAfter:
    phase: clusterInstalled

  extensions:
    - name: openshift-data-foundation-operator
