// Package entitlements resolves Environment entitlement declarations
// (community, Red Hat RHSM, IBM Storage Ceph) into the subscription,
// registry-credential, and license references consumers render or check.
// Secret material stays behind Environment secret names.
package entitlements
