// Package artifacts resolves the artifact-server InfraComponent selected by a
// cluster's artifact access, together with its listeners, endpoints, and
// resolved host, from a v1alpha1.State.
package artifacts
