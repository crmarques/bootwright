// Package advice emits non-blocking best-practice advisories over a
// validated State (for example storage layout notices). Advisories never
// fail validation; they are printed alongside it.
package advice
