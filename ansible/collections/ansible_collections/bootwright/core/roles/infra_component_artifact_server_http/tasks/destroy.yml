---
# Self-contained shared bastion service: one artifact-server container can be
# referenced by several Bootwright contexts. When the Go destroy planner finds another
# context still references this component it lists it in
# bootwright_infra_component_release_records; we then RELEASE it — skip every
# destructive step but still drop this context's ownership record — so the shared
# container and its published artifacts survive for the other contexts.
- name: Resolve shared-service release decision for boot artifact server
  ansible.builtin.set_fact:
    bootwright_component_release: >-
      {{ bootwright_infra_component_release_records is defined
         and (bootwright_component.providerName ~ '-' ~ bootwright_component.name)
             in bootwright_infra_component_release_records.split(',') }}
  changed_when: false

- name: Remove boot artifact server container
  containers.podman.podman_container:
    name: "{{ bootwright_artifacts_container_name }}"
    state: absent
  when: not bootwright_component_release

- name: Reap orphan boot artifact server from a leaked container instance
  ansible.builtin.include_role:
    name: bootwright.core.support_process_cleanup
  vars:
    bootwright_process_cleanup_podman_filters:
      - "label=bootwright.kind=artifacts"
      - "label=bootwright.provider={{ bootwright_component.providerName }}"
      - "label=bootwright.name={{ bootwright_component.name }}"
    bootwright_process_cleanup_mark_changed: false
  when: not bootwright_component_release

- name: Close boot artifact ports on host firewall
  ansible.posix.firewalld:
    port: "{{ item.port | int }}/tcp"
    state: disabled
    permanent: true
    immediate: true
  loop: "{{ bootwright_component.listeners | default([]) }}"
  loop_control:
    label: "{{ item.protocol }}:{{ item.port }}"
  when:
    - not bootwright_component_release
    - bootwright_firewalld_available | default(false)

- name: Remove boot artifact state directory
  ansible.builtin.file:
    path: "{{ bootwright_managed_services_dir }}/{{ bootwright_component.name }}"
    state: absent
  when: not bootwright_component_release

- name: Remove boot artifact ownership record
  ansible.builtin.include_role:
    name: bootwright.core.ownership_record
    tasks_from: remove_resource.yml
  vars:
    bootwright_ownership_kind: infra-component
    bootwright_ownership_name: "{{ bootwright_component.providerName }}-{{ bootwright_component.name }}"
