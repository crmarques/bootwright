---
- name: Skip BMC service (bmcRole=none)
  ansible.builtin.debug:
    msg: >-
      {{ bootwright_component.clusterName }}/{{ bootwright_component.name }}
      bmcRole=none — no provider BMC service to converge.
  changed_when: false
