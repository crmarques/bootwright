---
- name: Skip provider BMC service (bmcRole=redfish lives off-host)
  ansible.builtin.debug:
    msg: >-
      Provider {{ bootwright_component.providerName | default('unknown') }}: external Redfish BMCs;
      no provider-host convergence required.
  changed_when: false
