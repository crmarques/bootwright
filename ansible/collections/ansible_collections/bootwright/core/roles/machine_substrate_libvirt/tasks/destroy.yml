---
- name: Resolve libvirt destroy identifiers
  ansible.builtin.set_fact:
    bootwright_libvirt_machine_root: "{{ bootwright_clusters_dir }}/{{ bootwright_current_cluster.name }}/runtime/provider-state/libvirt/machines/{{ bootwright_component.name }}"
    bootwright_libvirt_storage_root: "/var/lib/libvirt/images/bootwright/{{ bootwright_clusters_dir | dirname | basename }}/clusters/{{ bootwright_current_cluster.name }}/machines/{{ bootwright_component.name }}"
  changed_when: false

- name: Read libvirt domain ownership metadata
  ansible.builtin.command:
    argv:
      - virsh
      - -c
      - "{{ bootwright_component.libvirt.uri | default('qemu:///system', true) }}"
      - dumpxml
      - "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"
  register: bootwright_libvirt_destroy_domain_xml
  changed_when: false
  failed_when: false

- name: Require a conclusive libvirt domain probe before destroy
  ansible.builtin.assert:
    that:
      - >-
        bootwright_libvirt_destroy_domain_xml.rc | default(1) == 0
        or (bootwright_libvirt_destroy_domain_xml.stderr | default('')) is search('(failed to get domain|Domain not found|no domain with matching name)')
    fail_msg: >-
      Could not determine whether libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}
      exists on {{ bootwright_component.libvirt.uri | default('qemu:///system', true) }}:
      {{ bootwright_libvirt_destroy_domain_xml.stderr | default('virsh dumpxml failed with no detail') }}.
      Refusing to tear down or delete anything Bootwright cannot conclusively
      probe — the domain may still exist while its disks, state, and ownership
      record would be removed. Fix libvirtd/connection access and re-run
      destroy.

- name: Resolve libvirt domain ownership for destroy
  ansible.builtin.set_fact:
    bootwright_libvirt_domain_present: "{{ bootwright_libvirt_destroy_domain_xml.rc | default(1) == 0 }}"
    bootwright_libvirt_destroy_owned: >-
      {{
        (bootwright_libvirt_destroy_domain_xml.stdout | default('')) is search('<([A-Za-z0-9_]+:)?context>' ~ (bootwright_clusters_dir | dirname | basename | regex_escape) ~ '</([A-Za-z0-9_]+:)?context>')
        and (bootwright_libvirt_destroy_domain_xml.stdout | default('')) is search('<([A-Za-z0-9_]+:)?cluster>' ~ (bootwright_current_cluster.name | regex_escape) ~ '</([A-Za-z0-9_]+:)?cluster>')
        and (bootwright_libvirt_destroy_domain_xml.stdout | default('')) is search('<([A-Za-z0-9_]+:)?machine>' ~ (bootwright_component.name | regex_escape) ~ '</([A-Za-z0-9_]+:)?machine>')
      }}
  changed_when: false

- name: Refuse to destroy a non-Bootwright libvirt domain
  ansible.builtin.assert:
    that:
      - bootwright_libvirt_destroy_owned | default(false) | bool
    fail_msg: >-
      Refusing to destroy libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}:
      it carries no Bootwright ownership marker for this context/cluster/machine,
      so it may be a foreign domain that happens to match the naming. Verify the
      domain before removing it manually, then retry, or pass --authorize unowned-vms to
      tear it down anyway.
  when:
    - bootwright_libvirt_domain_present | bool
    - not (bootwright_destroy_authorize_unowned_vms | default(false) | bool)

- name: Warn before force-destroying an unowned libvirt domain
  ansible.builtin.debug:
    msg: >-
      --authorize unowned-vms: destroying libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}
      despite a missing or mismatched Bootwright ownership marker for this
      context/cluster/machine.
  when:
    - bootwright_libvirt_domain_present | bool
    - bootwright_destroy_authorize_unowned_vms | default(false) | bool
    - not (bootwright_libvirt_destroy_owned | default(false) | bool)

- name: Stop libvirt domain
  ansible.builtin.command:
    argv:
      - virsh
      - -c
      - "{{ bootwright_component.libvirt.uri | default('qemu:///system', true) }}"
      - destroy
      - "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"
  register: bootwright_libvirt_destroy_domain_stop
  changed_when: bootwright_libvirt_destroy_domain_stop.rc == 0
  failed_when: false
  when: bootwright_libvirt_domain_present | bool

- name: Refuse disk deletion when the libvirt domain could not be stopped
  ansible.builtin.assert:
    that:
      - >-
        bootwright_libvirt_destroy_domain_stop.rc | default(1) == 0
        or (bootwright_libvirt_destroy_domain_stop.stderr | default(''))
           is search('(?i)(Domain not found|failed to get domain|no domain with matching name|domain is not running)')
    fail_msg: >-
      Refusing to delete disks, state, or ownership for libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }} because
      `virsh destroy` failed: rc
      {{ bootwright_libvirt_destroy_domain_stop.rc | default('<unknown>') }},
      stderr {{ bootwright_libvirt_destroy_domain_stop.stderr | default('') }}.
      Restore libvirt access or stop the domain safely, then retry the exact
      invocation `{{ bootwright_mutating_invocation | default('the same bootwright destroy invocation') }}`.
  when: bootwright_libvirt_domain_present | bool

- name: Undefine libvirt domain
  ansible.builtin.command:
    argv:
      - virsh
      - -c
      - "{{ bootwright_component.libvirt.uri | default('qemu:///system', true) }}"
      - undefine
      - "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"
      - --keep-nvram
  register: bootwright_libvirt_destroy_domain_undefine
  changed_when: bootwright_libvirt_destroy_domain_undefine.rc == 0
  failed_when: false
  when: bootwright_libvirt_domain_present | bool

- name: Refuse disk deletion when the libvirt domain could not be undefined
  ansible.builtin.assert:
    that:
      - >-
        bootwright_libvirt_destroy_domain_undefine.rc | default(1) == 0
        or (bootwright_libvirt_destroy_domain_undefine.stderr | default(''))
           is search('(?i)(Domain not found|failed to get domain|no domain with matching name)')
    fail_msg: >-
      Refusing to delete disks, state, or ownership for libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }} because
      `virsh undefine --keep-nvram` failed: rc
      {{ bootwright_libvirt_destroy_domain_undefine.rc | default('<unknown>') }},
      stderr {{ bootwright_libvirt_destroy_domain_undefine.stderr | default('') }}.
      Restore libvirt access or remove the domain safely, then retry the exact
      invocation `{{ bootwright_mutating_invocation | default('the same bootwright destroy invocation') }}`.
  when: bootwright_libvirt_domain_present | bool

- name: Verify the libvirt domain is absent before disk deletion
  ansible.builtin.command:
    argv:
      - virsh
      - -c
      - "{{ bootwright_component.libvirt.uri | default('qemu:///system', true) }}"
      - dumpxml
      - "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"
  register: bootwright_libvirt_destroy_domain_absence
  changed_when: false
  failed_when: false

- name: Require proven libvirt domain absence before disk deletion
  ansible.builtin.assert:
    that:
      - bootwright_libvirt_destroy_domain_absence.rc | default(0) != 0
      - >-
        (bootwright_libvirt_destroy_domain_absence.stderr | default(''))
        is search('(?i)(Domain not found|failed to get domain|no domain with matching name)')
    fail_msg: >-
      Refusing to delete disks, state, or ownership for libvirt domain
      {{ bootwright_current_cluster.name }}-{{ bootwright_component.name }} because
      its absence was not proven after teardown. rc
      {{ bootwright_libvirt_destroy_domain_absence.rc | default('<unknown>') }},
      stderr {{ bootwright_libvirt_destroy_domain_absence.stderr | default('') }}.
      Inspect the domain, restore libvirt access, and retry the exact invocation
      `{{ bootwright_mutating_invocation | default('the same bootwright destroy invocation') }}`.

- name: Remove machine state directory
  ansible.builtin.file:
    path: "{{ bootwright_libvirt_machine_root }}"
    state: absent

- name: Remove machine storage directory
  ansible.builtin.file:
    path: "{{ bootwright_libvirt_storage_root }}"
    state: absent

- name: Remove libvirt domain ownership record
  ansible.builtin.include_role:
    name: bootwright.core.ownership_record
    tasks_from: remove_resource.yml
  vars:
    bootwright_ownership_kind: libvirt-domain
    bootwright_ownership_name: "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"

- name: Remove managed OS install artifacts
  ansible.builtin.include_role:
    name: bootwright.core.ownership_record
    tasks_from: destroy_resource.yml
  vars:
    bootwright_ownership_record:
      kind: managed-os-install
      name: "{{ bootwright_current_cluster.name }}-{{ bootwright_component.name }}"
      paths:
        - "{{ bootwright_provider_state_dir }}/os-install/{{ bootwright_current_cluster.name }}/{{ bootwright_component.name }}"
      attributes: {}
  when: bootwright_component.osManaged | default(false) | bool

- name: Probe remaining managed-OS install machines in this cluster
  ansible.builtin.find:
    paths: "{{ bootwright_provider_state_dir }}/os-install/{{ bootwright_current_cluster.name }}"
    file_type: directory
    recurse: false
    excludes:
      - _source
  register: bootwright_libvirt_os_install_remaining
  when: bootwright_component.osManaged | default(false) | bool

- name: Remove per-cluster OS-install state after the last machine
  ansible.builtin.file:
    path: "{{ bootwright_provider_state_dir }}/os-install/{{ bootwright_current_cluster.name }}"
    state: absent
  when:
    - bootwright_component.osManaged | default(false) | bool
    - (bootwright_libvirt_os_install_remaining.matched | default(0)) == 0
