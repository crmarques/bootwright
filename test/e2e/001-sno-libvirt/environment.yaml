apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: sno-libvirt
spec:
  domains:
    base: bootwright.test

  infraComponents:
    nameResolution:
      - name: default
        management: managed
        componentRef: name-resolution
