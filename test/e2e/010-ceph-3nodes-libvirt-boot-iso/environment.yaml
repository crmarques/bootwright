apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: ceph-3nodes-libvirt-managed-os
spec:
  baseDomain: bootwright.test

  storageClusters:
    - ceph-libvirt

  infraComponents:
    nameResolution:
      - name: lab-dns
        management: managed
        componentRef: lab-dns
