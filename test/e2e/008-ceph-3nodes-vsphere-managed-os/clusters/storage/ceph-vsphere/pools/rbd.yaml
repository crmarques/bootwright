apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: rbd
spec:
  storageClusterRef: ceph-vsphere

  placementPolicyRef: lab-replicated

  ceph:
    type: replicated
    role: rbd
    application: rbd
