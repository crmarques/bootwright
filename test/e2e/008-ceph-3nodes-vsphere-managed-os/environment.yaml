apiVersion: bootwright.io/v1alpha1
kind: Environment
metadata:
  name: ceph-3nodes-vsphere-managed-os
spec:
  domains:
    base: bootwright.test

  storageClusters:
    - ceph-vsphere

  infraComponents:
    nameResolution:
      - name: lab-dns
        management: managed
        componentRef: lab-dns
