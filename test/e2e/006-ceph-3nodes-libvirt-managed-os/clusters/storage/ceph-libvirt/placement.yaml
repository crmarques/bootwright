apiVersion: bootwright.io/v1alpha1
kind: StoragePlacementPolicy
metadata:
  name: lab-replicated
spec:
  storageClusterRef:
    name: ceph-libvirt

  ceph:
    failureDomain: host
    ruleName: lab-replicated
    replicated:
      size: 3
      minSize: 2
