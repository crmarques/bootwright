apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: rgw
spec:
  storageClusterRef:
    name: ceph-libvirt

  placementPolicyRef:
    name: lab-replicated

  ceph:
    type: replicated
    role: rgw
    application: rgw
