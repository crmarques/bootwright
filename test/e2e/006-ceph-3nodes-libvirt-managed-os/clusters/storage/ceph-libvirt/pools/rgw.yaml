apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: rgw
spec:
  storageClusterRef: ceph-libvirt

  placementPolicyRef: lab-replicated

  ceph:
    type: replicated
    role: rgw
    application: rgw
