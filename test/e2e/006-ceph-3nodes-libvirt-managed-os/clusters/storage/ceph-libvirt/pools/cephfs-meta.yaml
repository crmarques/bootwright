apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: cephfs-meta
spec:
  storageClusterRef:
    name: ceph-libvirt

  placementPolicyRef:
    name: lab-replicated

  ceph:
    type: replicated
    role: cephfs-metadata
    application: cephfs
