apiVersion: bootwright.io/v1alpha1
kind: StoragePool
metadata:
  name: cephfs-data
spec:
  storageClusterRef:
    name: ceph-libvirt

  placementPolicyRef:
    name: lab-replicated

  ceph:
    type: replicated
    role: cephfs-data
    application: cephfs
